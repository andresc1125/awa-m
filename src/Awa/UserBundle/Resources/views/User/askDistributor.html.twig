{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Distributor creation</h1>

    <form action="{{ path('distributor_user_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>
{% endblock %}
